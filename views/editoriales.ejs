<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>BiblioApp - Editoriales</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      background: url('https://images.unsplash.com/photo-1519682337058-a94d519337bc') no-repeat center center fixed;
      background-size: cover;
      color: white;
    }
    .card {
      background-color: rgba(0, 0, 0, 0.75);
      color: white;
    }
    .card-header {
      background-color: rgba(255, 255, 255, 0.9);
    }
    .btn-add-custom {
      background: linear-gradient(45deg, #007bff, #6610f2);
      color: white;
      border: none;
      padding: 8px 20px;
      font-weight: bold;
      border-radius: 30px;
      transition: 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }
    .btn-add-custom.btn-sm {
      padding: 8px 22px;
      font-size: 0.8rem;
    }
    .btn-add-custom:hover {
      background: linear-gradient(45deg, #6610f2, #007bff);
      color: #fff;
      text-decoration: none;
    }
    .sidebar {
      background-color: rgba(0, 0, 0, 0.8);
      min-height: 100vh;
    }
    .nav-link {
      color: #fff;
    }
    .nav-link:hover {
      color: #ffc107;
    }
    footer {
      background-color: rgba(0, 0, 0, 0.8);
    }
    .avatar-small {
      width: 40px;
      height: 40px;
      object-fit: cover;
      border-radius: 50%;
      display: inline-block;
    }
    .modal-content {
      background-color: #000;
      color: #fff;
      border: 1px solid #444;
    }
    .modal-header, .modal-footer {
      border-color: #444;
    }
    .btn-close {
      filter: invert(1);
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark w-100" style="margin-bottom: 0; border-radius: 0;">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <span class="font-weight-bold">📚 Mi Biblioteca</span>
    </a>
  </nav>

  <!-- Contenedor principal -->
  <div class="d-flex" style="min-height: 100vh;">
    <!-- Sidebar -->
    <div class="sidebar p-3 text-white" style="width: 250px;">
      <div class="text-center mb-4">
        <img 
          src="/images/obed.png" 
          alt="Usuario hombre" 
          class="avatar-small mb-2" 
        />
        <h6>Obed Alvarez</h6>
        <small class="text-success">● En línea</small>
      </div>
      <ul class="nav flex-column">
        <li class="nav-item mb-2"><a class="nav-link" href="/">📊 Dashboard</a></li>
        <li class="nav-item mb-2"><a class="nav-link" href="/books/autores">🖋 Autores</a></li>
        <li class="nav-item"><a class="nav-link active" href="/books/editoriales">🏢 Editoriales</a></li>
        <li class="nav-item"><a class="nav-link" href="/books/categorias">📂 Categorias</a></li>
        <li class="nav-item"><a class="nav-link" href="/books">📖 Libros</a></li>
        <li class="nav-item"><a class="nav-link active" href="/books/prestamos">📅 Préstamos</a></li>
      </ul>
    </div>

    <!-- Contenido principal -->
    <div class="container mt-4">
      <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center text-dark">
          <span class="text-dark w-100 text-center" style="font-weight:bold; font-size:1.5rem;">Gestión de Editoriales</span>
          <!-- Botón para abrir el modal de agregar -->
          <button class="btn-add-custom btn-sm" data-bs-toggle="modal" data-bs-target="#modalAgregarEditorial">
            <i class="bi bi-plus-circle"></i> Nueva Editorial
          </button>
        </div>
        <div class="card-body">
          <input type="text" class="form-control mb-3" placeholder="Buscar editoriales nombre o estado...">
          <table class="table table-bordered table-hover table-dark">
            <thead class="table-light">
              <tr>
                <th>#</th>
                <th>Nombre</th>
                <th>Estado</th>
                <th>Acción</th>
              </tr>
            </thead>
            <tbody>
              <% editorials.forEach((editorial, index) => { %>
                <tr>
                  <td><%= index + 1 %></td>
                  <td><%= editorial.name %></td>
                  <td><%= editorial.status %></td>
                  <td>
                    <!-- Botón para abrir modal de editar -->
                    <button 
                      class="btn btn-primary btn-sm btn-editar"
                      data-id="<%= editorial.id %>"
                      data-name="<%= editorial.name %>"
                      data-status="<%= editorial.status %>"
                      data-bs-toggle="modal"
                      data-bs-target="#modalEditarEditorial"
                    >✏️ Editar</button>
                    <!-- Botón para abrir modal de eliminar -->
                    <button 
                      class="btn btn-danger btn-sm btn-eliminar"
                      data-id="<%= editorial.id %>"
                      data-name="<%= editorial.name %>"
                      data-bs-toggle="modal"
                      data-bs-target="#modalEliminarEditorial"
                    >🗑️ Eliminar</button>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <!-- Modal para agregar editorial -->
  <div class="modal fade" id="modalAgregarEditorial" tabindex="-1" aria-labelledby="modalAgregarEditorialLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content text-white">
        <div class="modal-header">
          <h5 class="modal-title" id="modalAgregarEditorialLabel">Nueva Editorial</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <form action="/books/editoriales/add" method="POST">
          <div class="modal-body">
            <div class="mb-3">
              <label for="name" class="form-label">Nombre:</label>
              <input type="text" class="form-control" id="name" name="name" required autocomplete="off">
            </div>
            <div class="mb-3">
              <label for="status" class="form-label">Estado:</label>
              <select class="form-control" id="status" name="status" required>
                <option value="Activo">Activo</option>
                <option value="Inactivo">Inactivo</option>
              </select>
            </div>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-primary">Guardar</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Modal para editar editorial -->
  <div class="modal fade" id="modalEditarEditorial" tabindex="-1" aria-labelledby="modalEditarEditorialLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content text-white">
        <div class="modal-header">
          <h5 class="modal-title" id="modalEditarEditorialLabel">Editar Editorial</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <form id="formEditarEditorial" method="POST">
          <div class="modal-body">
            <div class="mb-3">
              <label for="edit-name" class="form-label">Nombre:</label>
              <input type="text" class="form-control" id="edit-name" name="name" required autocomplete="off">
            </div>
            <div class="mb-3">
              <label for="edit-status" class="form-label">Estado:</label>
              <select class="form-control" id="edit-status" name="status" required>
                <option value="Activo">Activo</option>
                <option value="Inactivo">Inactivo</option>
              </select>
            </div>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-primary">Actualizar</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Modal para eliminar editorial -->
  <div class="modal fade" id="modalEliminarEditorial" tabindex="-1" aria-labelledby="modalEliminarEditorialLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content text-white">
        <div class="modal-header">
          <h5 class="modal-title" id="modalEliminarEditorialLabel">Eliminar Editorial</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <form id="formEliminarEditorial" method="POST">
          <div class="modal-body">
            <p>¿Seguro que deseas eliminar la editorial <strong id="delete-nombre"></strong>?</p>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-danger">Eliminar</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS (para el modal) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Editar: cargar datos en el modal
    document.querySelectorAll('.btn-editar').forEach(btn => {
      btn.addEventListener('click', function() {
        const id = this.getAttribute('data-id');
        const name = this.getAttribute('data-name');
        const status = this.getAttribute('data-status');
        document.getElementById('edit-name').value = name;
        document.getElementById('edit-status').value = status;
        document.getElementById('formEditarEditorial').action = `/books/editoriales/edit/${id}`;
      });
    });

    // Eliminar: cargar datos en el modal
    document.querySelectorAll('.btn-eliminar').forEach(btn => {
      btn.addEventListener('click', function() {
        const id = this.getAttribute('data-id');
        const name = this.getAttribute('data-name');
        document.getElementById('delete-nombre').textContent = name;
        document.getElementById('formEliminarEditorial').action = `/books/editoriales/delete/${id}`;
      });
    });
  </script>
  <!-- Footer fijo abajo -->
  <footer class="text-white text-center py-3 mt-auto w-100">
    &copy; 2025 Mi Biblioteca. Todos los derechos reservados.
    <span class="float-right mr-4">Versión 1.0</span>
  </footer>
</body>
</html>