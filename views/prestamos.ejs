<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>BiblioApp - Préstamos</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      background: url('https://images.unsplash.com/photo-1519682337058-a94d519337bc') no-repeat center center fixed;
      background-size: cover;
      color: white;
    }
    .card {
      background-color: rgba(0, 0, 0, 0.75);
      color: white;
    }
    .card-header {
      background-color: rgba(255, 255, 255, 0.9);
    }
    .btn-add-custom {
      background: linear-gradient(45deg, #007bff, #6610f2);
      color: white;
      border: none;
      padding: 8px 20px;
      font-weight: bold;
      border-radius: 30px;
      transition: 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }
    .btn-add-custom.btn-sm {
      padding: 8px 22px;
      font-size: 0.8rem;
    }
    .btn-add-custom:hover {
      background: linear-gradient(45deg, #6610f2, #007bff);
      color: #fff;
      text-decoration: none;
    }
    .sidebar {
      background-color: rgba(0, 0, 0, 0.8);
      min-height: 100vh;
    }
    .nav-link {
      color: #fff;
    }
    .nav-link:hover {
      color: #ffc107;
    }
    .avatar-small {
      width: 40px;
      height: 40px;
      object-fit: cover;
      border-radius: 50%;
      display: inline-block;
    }
    .modal-content {
      background-color: #000;
      color: #fff;
      border: 1px solid #444;
    }
    .modal-header, .modal-footer {
      border-color: #444;
    }
    .btn-close {
      filter: invert(1);
    }
    .table th, .table td {
      color: #fff;
    }
    footer {
      background-color: rgba(0, 0, 0, 0.8);
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark w-100" style="margin-bottom: 0; border-radius: 0;">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <span class="font-weight-bold">📚 Mi Biblioteca</span>
    </a>
  </nav>

  <div class="d-flex" style="min-height: 100vh;">
    <!-- Sidebar -->
    <div class="sidebar p-3 text-white" style="width: 250px;">
      <div class="text-center mb-4">
        <img src="/images/obed.png" alt="Usuario hombre" class="avatar-small mb-2" />
        <h6>Obed Alvarez</h6>
        <small class="text-success">● En línea</small>
      </div>
      <ul class="nav flex-column">
        <li class="nav-item mb-2"><a class="nav-link" href="/">📊 Dashboard</a></li>
        <li class="nav-item mb-2"><a class="nav-link" href="/books/autores">🖋 Autores</a></li>
        <li class="nav-item"><a class="nav-link" href="/books/editoriales">🏢 Editoriales</a></li>
        <li class="nav-item"><a class="nav-link" href="/books/categorias">📂 Categorias</a></li>
        <li class="nav-item"><a class="nav-link" href="/books">📖 Libros</a></li>
        <li class="nav-item"><a class="nav-link active" href="/books/prestamos">📅 Préstamos</a></li>
      </ul>
    </div>

    <!-- Contenido principal -->
    <div class="container mt-4">
      <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center text-dark">
          <span class="text-dark w-100 text-center" style="font-weight:bold; font-size:1.5rem;">Gestión de Préstamos</span>
          <button class="btn-add-custom btn-sm" data-bs-toggle="modal" data-bs-target="#modalAgregarPrestamo">
            <i class="bi bi-plus-circle"></i> Nuevo Préstamo
          </button>
        </div>
        <div class="card-body">
          <table class="table table-bordered table-hover table-dark">
            <thead class="table-light">
              <tr>
                <th class="text-dark">#</th>
                <th class="text-dark">Libro</th>
                <th class="text-dark">Usuario</th>
                <th class="text-dark">Fecha Préstamo</th>
                <th class="text-dark">Fecha Devolución</th>
                <th class="text-dark">Estado</th>
                <th class="text-dark">Acciones</th>
              </tr>
            </thead>
            <tbody>
              <% if (prestamos && prestamos.length > 0) { %>
                <% prestamos.forEach((prestamo, i) => { %>
                  <tr>
                    <td><%= i + 1 %></td>
                    <td><%= prestamo.libro_nombre %></td>
                    <td><%= prestamo.usuario_nombre %></td>
                    <td><%= prestamo.fecha_prestamo %></td>
                    <td><%= prestamo.fecha_devolucion %></td>
                    <td><%= prestamo.estado %></td>
                    <td>
                      <button class="btn btn-primary btn-sm btn-editar"
                        data-id="<%= prestamo.id %>"
                        data-libro="<%= prestamo.libro_id %>"
                        data-usuario="<%= prestamo.usuario_id %>"
                        data-fecha_prestamo="<%= prestamo.fecha_prestamo %>"
                        data-fecha_devolucion="<%= prestamo.fecha_devolucion %>"
                        data-estado="<%= prestamo.estado %>"
                        data-bs-toggle="modal"
                        data-bs-target="#modalEditarPrestamo">
                        ✏️ Editar
                      </button>
                      <button class="btn btn-danger btn-sm btn-eliminar"
                        data-id="<%= prestamo.id %>"
                        data-libro="<%= prestamo.libro_nombre %>"
                        data-bs-toggle="modal"
                        data-bs-target="#modalEliminarPrestamo">
                        🗑️ Eliminar
                      </button>
                    </td>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr><td colspan="7" class="text-center">No hay préstamos registrados.</td></tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <!-- Modal para agregar préstamo -->
  <div class="modal fade" id="modalAgregarPrestamo" tabindex="-1" aria-labelledby="modalAgregarPrestamoLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content text-white">
        <div class="modal-header">
          <h5 class="modal-title" id="modalAgregarPrestamoLabel">Nuevo Préstamo</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <form action="/books/prestamos/add" method="POST">
          <div class="modal-body">
            <div class="mb-3">
              <label for="libro" class="form-label">Libro:</label>
              <select class="form-select" id="libro" name="libro" required>
                <option value="">Seleccione un libro</option>
                <% libros.forEach(libro => { %>
                  <option value="<%= libro.id %>"><%= libro.name %></option>
                <% }) %>
              </select>
            </div>
            <div class="mb-3">
              <label for="usuario" class="form-label">Usuario:</label>
              <select class="form-select" id="usuario" name="usuario" required>
                <option value="">Seleccione un usuario</option>
                <% usuarios.forEach(usuario => { %>
                  <option value="<%= usuario.id %>"><%= usuario.nombre %></option>
                <% }) %>
              </select>
            </div>
            <div class="mb-3">
              <label for="fecha_prestamo" class="form-label">Fecha Préstamo:</label>
              <input type="date" class="form-control" id="fecha_prestamo" name="fecha_prestamo" required>
            </div>
            <div class="mb-3">
              <label for="fecha_devolucion" class="form-label">Fecha Devolución:</label>
              <input type="date" class="form-control" id="fecha_devolucion" name="fecha_devolucion" required>
            </div>
            <div class="mb-3">
              <label for="estado" class="form-label">Estado:</label>
              <select class="form-select" id="estado" name="estado" required>
                <option value="Prestado">Prestado</option>
                <option value="Devuelto">Devuelto</option>
                <option value="Vencido">Vencido</option>
              </select>
            </div>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-primary">Guardar</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Modal para editar préstamo -->
  <div class="modal fade" id="modalEditarPrestamo" tabindex="-1" aria-labelledby="modalEditarPrestamoLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content text-white">
        <div class="modal-header">
          <h5 class="modal-title" id="modalEditarPrestamoLabel">Editar Préstamo</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <form id="formEditarPrestamo" method="POST">
          <div class="modal-body">
            <div class="mb-3">
              <label for="edit-libro" class="form-label">Libro:</label>
              <select class="form-select" id="edit-libro" name="libro" required>
                <option value="">Seleccione un libro</option>
                <% libros.forEach(libro => { %>
                  <option value="<%= libro.id %>"><%= libro.name %></option>
                <% }) %>
              </select>
            </div>
            <div class="mb-3">
              <label for="edit-usuario" class="form-label">Usuario:</label>
              <select class="form-select" id="edit-usuario" name="usuario" required>
                <option value="">Seleccione un usuario</option>
                <% usuarios.forEach(usuario => { %>
                  <option value="<%= usuario.id %>"><%= usuario.nombre %></option>
                <% }) %>
              </select>
            </div>
            <div class="mb-3">
              <label for="edit-fecha_prestamo" class="form-label">Fecha Préstamo:</label>
              <input type="date" class="form-control" id="edit-fecha_prestamo" name="fecha_prestamo" required>
            </div>
            <div class="mb-3">
              <label for="edit-fecha_devolucion" class="form-label">Fecha Devolución:</label>
              <input type="date" class="form-control" id="edit-fecha_devolucion" name="fecha_devolucion" required>
            </div>
            <div class="mb-3">
              <label for="edit-estado" class="form-label">Estado:</label>
              <select class="form-select" id="edit-estado" name="estado" required>
                <option value="Prestado">Prestado</option>
                <option value="Devuelto">Devuelto</option>
                <option value="Vencido">Vencido</option>
              </select>
            </div>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-primary">Actualizar</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Modal para eliminar préstamo -->
  <div class="modal fade" id="modalEliminarPrestamo" tabindex="-1" aria-labelledby="modalEliminarPrestamoLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content text-white">
        <div class="modal-header">
          <h5 class="modal-title" id="modalEliminarPrestamoLabel">Eliminar Préstamo</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <form id="formEliminarPrestamo" method="POST">
          <div class="modal-body">
            <p>¿Seguro que deseas eliminar el préstamo del libro <strong id="delete-libro-nombre"></strong>?</p>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-danger">Eliminar</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Editar: cargar datos en el modal
    document.querySelectorAll('.btn-editar').forEach(btn => {
      btn.addEventListener('click', function() {
        const id = this.getAttribute('data-id');
        const libro = this.getAttribute('data-libro');
        const usuario = this.getAttribute('data-usuario');
        const fecha_prestamo = this.getAttribute('data-fecha_prestamo');
        const fecha_devolucion = this.getAttribute('data-fecha_devolucion');
        const estado = this.getAttribute('data-estado');
        document.getElementById('edit-libro').value = libro;
        document.getElementById('edit-usuario').value = usuario;
        document.getElementById('edit-fecha_prestamo').value = fecha_prestamo;
        document.getElementById('edit-fecha_devolucion').value = fecha_devolucion;
        document.getElementById('edit-estado').value = estado;
        document.getElementById('formEditarPrestamo').action = `/books/prestamos/edit/${id}`;
      });
    });

    // Eliminar: cargar datos en el modal
    document.querySelectorAll('.btn-eliminar').forEach(btn => {
      btn.addEventListener('click', function() {
        const id = this.getAttribute('data-id');
        const libro = this.getAttribute('data-libro');
        document.getElementById('delete-libro-nombre').textContent = libro;
        document.getElementById('formEliminarPrestamo').action = `/books/prestamos/delete/${id}`;
      });
    });
  </script>
  <footer class="text-white text-center py-3 mt-auto w-100">
    &copy; 2025 Mi Biblioteca. Todos los derechos reservados.
    <span class="float-right mr-4">Versión 1.0</span>
  </footer>
</body>
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>BiblioApp - Préstamos</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      background: url('https://images.unsplash.com/photo-1519682337058-a94d519337bc') no-repeat center center fixed;
      background-size: cover;
      color: white;
    }
    .card {
      background-color: rgba(0, 0, 0, 0.75);
      color: white;
    }
    .card-header {
      background-color: rgba(255, 255, 255, 0.9);
    }
    .btn-add-custom {
      background: linear-gradient(45deg, #007bff, #6610f2);
      color: white;
      border: none;
      padding: 8px 20px;
      font-weight: bold;
      border-radius: 30px;
      transition: 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }
    .btn-add-custom.btn-sm {
      padding: 8px 22px;
      font-size: 0.8rem;
    }
    .btn-add-custom:hover {
      background: linear-gradient(45deg, #6610f2, #007bff);
      color: #fff;
      text-decoration: none;
    }
    .sidebar {
      background-color: rgba(0, 0, 0, 0.8);
      min-height: 100vh;
    }
    .nav-link {
      color: #fff;
    }
    .nav-link:hover {
      color: #ffc107;
    }
    .avatar-small {
      width: 40px;
      height: 40px;
      object-fit: cover;
      border-radius: 50%;
      display: inline-block;
    }
    .modal-content {
      background-color: #000;
      color: #fff;
      border: 1px solid #444;
    }
    .modal-header, .modal-footer {
      border-color: #444;
    }
    .btn-close {
      filter: invert(1);
    }
    .table th, .table td {
      color: #fff;
    }
    .btn-edit-custom {
      background: linear-gradient(45deg, #007bff, #6610f2);
      color: #fff;
      border: none;
      margin-right: 6px;
      border-radius: 20px;
      padding: 4px 18px;
      font-weight: 500;
      transition: 0.2s;
    }
    .btn-edit-custom:hover {
      background: linear-gradient(45deg, #6610f2, #007bff);
      color: #fff;
    }
    .btn-delete-custom {
      background: #dc3545;
      color: #fff;
      border: none;
      border-radius: 20px;
      padding: 4px 18px;
      font-weight: 500;
      transition: 0.2s;
    }
    .btn-delete-custom:hover {
      background: #b52a37;
      color: #fff;
    }
    footer {
      background-color: rgba(0, 0, 0, 0.8);
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark w-100" style="margin-bottom: 0; border-radius: 0;">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <span class="font-weight-bold">📚 Mi Biblioteca</span>
    </a>
  </nav>

  <div class="d-flex" style="min-height: 100vh;">
    <!-- Sidebar -->
    <div class="sidebar p-3 text-white" style="width: 250px;">
      <div class="text-center mb-4">
        <img src="/images/obed.png" alt="Usuario hombre" class="avatar-small mb-2" />
        <h6>Obed Alvarez</h6>
        <small class="text-success">● En línea</small>
      </div>
      <ul class="nav flex-column">
        <li class="nav-item mb-2"><a class="nav-link" href="/">📊 Dashboard</a></li>
        <li class="nav-item mb-2"><a class="nav-link" href="/books/autores">🖋 Autores</a></li>
        <li class="nav-item"><a class="nav-link" href="/books/editoriales">🏢 Editoriales</a></li>
        <li class="nav-item"><a class="nav-link" href="/books/categorias">📂 Categorias</a></li>
        <li class="nav-item"><a class="nav-link" href="/books">📖 Libros</a></li>
        <li class="nav-item"><a class="nav-link active" href="/books/prestamos">📅 Préstamos</a></li>
      </ul>
    </div>

    <!-- Contenido principal -->
    <div class="container mt-4">
      <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center text-dark">
          <span class="text-dark w-100 text-center" style="font-weight:bold; font-size:1.5rem;">Gestión de Préstamos</span>
          <button class="btn-add-custom btn-sm" data-bs-toggle="modal" data-bs-target="#modalAgregarPrestamo">
            <i class="bi bi-plus-circle"></i> Nuevo Préstamo
          </button>
        </div>
        <div class="card-body">
          <table class="table table-bordered table-hover table-dark">
            <thead class="table-light">
              <tr>
                <th class="text-dark">#</th>
                <th class="text-dark">Libro</th>
                <th class="text-dark">Usuario</th>
                <th class="text-dark">Fecha Préstamo</th>
                <th class="text-dark">Fecha Devolución</th>
                <th class="text-dark">Estado</th>
                <th class="text-dark">Acciones</th>
              </tr>
            </thead>
            <tbody>
              <% if (prestamos && prestamos.length > 0) { %>
                <% prestamos.forEach((prestamo, i) => { %>
                  <tr>
                    <td><%= i + 1 %></td>
                    <td><%= prestamo.libro_nombre %></td>
                    <td><%= prestamo.usuario_nombre %></td>
                   <td>
                    <%= prestamo.fecha_prestamo ? prestamo.fecha_prestamo.substring(0,10) : '' %>
                  </td>
                  <td>
                    <%= prestamo.fecha_devolucion ? prestamo.fecha_devolucion.substring(0,10) : '' %>
                  </td>
                    <td><%= prestamo.estado %></td>
                    <td>
                      <div class="d-flex">
                        <button class="btn btn-edit-custom btn-sm btn-editar"
                          data-id="<%= prestamo.id %>"
                          data-libro="<%= prestamo.libro_id %>"
                          data-usuario="<%= prestamo.usuario_id %>"
                          data-fecha_prestamo="<%= prestamo.fecha_prestamo ? (new Date(prestamo.fecha_prestamo)).toISOString().slice(0,10) : '' %>"
                          data-fecha_devolucion="<%= prestamo.fecha_devolucion ? (new Date(prestamo.fecha_devolucion)).toISOString().slice(0,10) : '' %>"
                          data-estado="<%= prestamo.estado %>"
                          data-bs-toggle="modal"
                          data-bs-target="#modalEditarPrestamo">
                          <i class="bi bi-pencil-square"></i> Editar
                        </button>
                        <button class="btn btn-delete-custom btn-sm btn-eliminar"
                          data-id="<%= prestamo.id %>"
                          data-libro="<%= prestamo.libro_nombre %>"
                          data-bs-toggle="modal"
                          data-bs-target="#modalEliminarPrestamo">
                          <i class="bi bi-trash"></i> Eliminar
                        </button>
                      </div>
                    </td>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr><td colspan="7" class="text-center">No hay préstamos registrados.</td></tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <!-- Modal para agregar préstamo -->
  <div class="modal fade" id="modalAgregarPrestamo" tabindex="-1" aria-labelledby="modalAgregarPrestamoLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content text-white">
        <div class="modal-header">
          <h5 class="modal-title" id="modalAgregarPrestamoLabel">Nuevo Préstamo</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <form action="/books/prestamos/add" method="POST">
          <div class="modal-body">
            <div class="mb-3">
              <label for="libro" class="form-label">Libro:</label>
              <select class="form-select" id="libro" name="libro" required>
                <option value="">Seleccione un libro</option>
                <% libros.forEach(libro => { %>
                  <option value="<%= libro.id %>"><%= libro.name %></option>
                <% }) %>
              </select>
            </div>
            <div class="mb-3">
              <label for="usuario" class="form-label">Usuario:</label>
              <select class="form-select" id="usuario" name="usuario" required>
                <option value="">Seleccione un usuario</option>
                <% usuarios.forEach(usuario => { %>
                  <option value="<%= usuario.id %>"><%= usuario.nombre %></option>
                <% }) %>
              </select>
            </div>
            <div class="mb-3">
              <label for="fecha_prestamo" class="form-label">Fecha Préstamo:</label>
              <input type="date" class="form-control" id="fecha_prestamo" name="fecha_prestamo" required>
            </div>
            <div class="mb-3">
              <label for="fecha_devolucion" class="form-label">Fecha Devolución:</label>
              <input type="date" class="form-control" id="fecha_devolucion" name="fecha_devolucion" required>
            </div>
            <div class="mb-3">
              <label for="estado" class="form-label">Estado:</label>
              <select class="form-select" id="estado" name="estado" required>
                <option value="Prestado">Prestado</option>
                <option value="Devuelto">Devuelto</option>
                <option value="Vencido">Vencido</option>
              </select>
            </div>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-primary">Guardar</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Modal para editar préstamo -->
  <div class="modal fade" id="modalEditarPrestamo" tabindex="-1" aria-labelledby="modalEditarPrestamoLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content text-white">
        <div class="modal-header">
          <h5 class="modal-title" id="modalEditarPrestamoLabel">Editar Préstamo</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <form id="formEditarPrestamo" method="POST">
          <div class="modal-body">
            <div class="mb-3">
              <label for="edit-libro" class="form-label">Libro:</label>
              <select class="form-select" id="edit-libro" name="libro" required>
                <option value="">Seleccione un libro</option>
                <% libros.forEach(libro => { %>
                  <option value="<%= libro.id %>"><%= libro.name %></option>
                <% }) %>
              </select>
            </div>
            <div class="mb-3">
              <label for="edit-usuario" class="form-label">Usuario:</label>
              <select class="form-select" id="edit-usuario" name="usuario" required>
                <option value="">Seleccione un usuario</option>
                <% usuarios.forEach(usuario => { %>
                  <option value="<%= usuario.id %>"><%= usuario.nombre %></option>
                <% }) %>
              </select>
            </div>
            <div class="mb-3">
              <label for="edit-fecha_prestamo" class="form-label">Fecha Préstamo:</label>
              <input type="date" class="form-control" id="edit-fecha_prestamo" name="fecha_prestamo" required>
            </div>
            <div class="mb-3">
              <label for="edit-fecha_devolucion" class="form-label">Fecha Devolución:</label>
              <input type="date" class="form-control" id="edit-fecha_devolucion" name="fecha_devolucion" required>
            </div>
            <div class="mb-3">
              <label for="edit-estado" class="form-label">Estado:</label>
              <select class="form-select" id="edit-estado" name="estado" required>
                <option value="Prestado">Prestado</option>
                <option value="Devuelto">Devuelto</option>
                <option value="Vencido">Vencido</option>
              </select>
            </div>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-primary">Actualizar</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Modal para eliminar préstamo -->
  <div class="modal fade" id="modalEliminarPrestamo" tabindex="-1" aria-labelledby="modalEliminarPrestamoLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content text-white">
        <div class="modal-header">
          <h5 class="modal-title" id="modalEliminarPrestamoLabel">Eliminar Préstamo</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <form id="formEliminarPrestamo" method="POST">
          <div class="modal-body">
            <p>¿Seguro que deseas eliminar el préstamo del libro <strong id="delete-libro-nombre"></strong>?</p>
          </div>
          <div class="modal-footer">
            <button type="submit" class="btn btn-danger">Eliminar</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Editar: cargar datos en el modal
    document.querySelectorAll('.btn-editar').forEach(btn => {
      btn.addEventListener('click', function() {
        const id = this.getAttribute('data-id');
        const libro = this.getAttribute('data-libro');
        const usuario = this.getAttribute('data-usuario');
        const fecha_prestamo = this.getAttribute('data-fecha_prestamo');
        const fecha_devolucion = this.getAttribute('data-fecha_devolucion');
        const estado = this.getAttribute('data-estado');
        document.getElementById('edit-libro').value = libro;
        document.getElementById('edit-usuario').value = usuario;
        document.getElementById('edit-fecha_prestamo').value = fecha_prestamo;
        document.getElementById('edit-fecha_devolucion').value = fecha_devolucion;
        document.getElementById('edit-estado').value = estado;
        document.getElementById('formEditarPrestamo').action = `/books/prestamos/edit/${id}`;
      });
    });

    // Eliminar: cargar datos en el modal
    document.querySelectorAll('.btn-eliminar').forEach(btn => {
      btn.addEventListener('click', function() {
        const id = this.getAttribute('data-id');
        const libro = this.getAttribute('data-libro');
        document.getElementById('delete-libro-nombre').textContent = libro;
        document.getElementById('formEliminarPrestamo').action = `/books/prestamos/delete/${id}`;
      });
    });
  </script>
  <footer class="text-white text-center py-3 mt-auto w-100">
    &copy; 2025 Mi Biblioteca. Todos los derechos reservados.
    <span class="float-right mr-4">Versión 1.0</span>
  </footer>
</body>
</html>